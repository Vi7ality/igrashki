export type ClientValuesType = {
  parentName: string;
  parentSurname: string;
  phoneNumber: string;
  password: string;
  childName: string;
  childBirthDate: string;
  messenger: string;
  formType: string;
  acceptRules: boolean;
};

export type ClientState = {
  messenger: string;
  acceptRules: boolean;
}